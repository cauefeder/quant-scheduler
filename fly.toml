app = "quant-scheduler"
primary_region = "ord"   # Chicago — closest to Saskatchewan

[build]

[env]
  PYTHONUTF8       = "1"
  PYTHONIOENCODING = "utf-8"

# Pure worker — no HTTP service, no ports
[[vm]]
  memory   = "256mb"
  cpu_kind = "shared"
  cpus     = 1
